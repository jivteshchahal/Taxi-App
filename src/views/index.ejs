<!-- Hero Section - Book Your Taxi -->
<section class="relative py-20 overflow-hidden">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-gradient-to-br from-emerald-600 via-teal-700 to-cyan-800 rounded-3xl shadow-2xl p-12 text-center relative overflow-hidden">
      <!-- Decorative bubbles -->
      <div class="pointer-events-none absolute inset-0 opacity-20">
        <div class="absolute top-8 left-8 w-32 h-32 bg-gradient-to-br from-yellow-300 to-orange-400 rounded-full blur-xl"></div>
        <div class="absolute top-20 right-12 w-24 h-24 bg-gradient-to-br from-pink-300 to-purple-400 rounded-full blur-lg"></div>
        <div class="absolute bottom-16 left-16 w-20 h-20 bg-gradient-to-br from-blue-300 to-indigo-400 rounded-full blur-lg"></div>
        <div class="absolute bottom-8 right-8 w-36 h-36 bg-gradient-to-br from-green-300 to-emerald-400 rounded-full blur-xl"></div>
        <div class="absolute top-1/2 left-1/4 w-16 h-16 bg-gradient-to-br from-yellow-200 to-amber-300 rounded-full blur-md"></div>
        <div class="absolute top-1/3 right-1/3 w-28 h-28 bg-gradient-to-br from-purple-200 to-pink-300 rounded-full blur-lg"></div>
      </div>

      <div class="relative z-10">
        <div class="mb-8">
          <div class="relative mx-auto w-80 h-48 md:w-96 md:h-60 rounded-2xl overflow-hidden shadow-2xl">
            <img
              src="https://images.unsplash.com/photo-1544620347-c4fd4a3d5957?auto=format&fit=crop&w=2069&q=80"
              alt="Classic yellow taxi light sign"
              class="w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
          </div>
        </div>

        <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 leading-tight">Book Your Taxi</h1>
        <p class="text-xl md:text-2xl text-teal-100 mb-8 max-w-2xl mx-auto">Experience premium transportation with just a few clicks. Fast, reliable, and available 24/7.</p>
        <a href="#booking" class="inline-block bg-yellow-400 hover:bg-yellow-300 text-gray-900 font-bold text-xl px-10 py-4 rounded-full transition transform hover:scale-105 shadow-lg hover:shadow-xl focus:outline-none focus:ring-4 focus:ring-yellow-300">Start Booking Now →</a>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto mt-10">
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 text-white">
            <div class="text-4xl mb-3">🚀</div>
            <h3 class="text-xl font-semibold mb-2">Instant Booking</h3>
            <p class="text-teal-100">Book in under 60 seconds with our streamlined process</p>
          </div>
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 text-white">
            <div class="text-4xl mb-3">⭐</div>
            <h3 class="text-xl font-semibold mb-2">5-Star Service</h3>
            <p class="text-teal-100">Professional drivers with excellent customer ratings</p>
          </div>
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 text-white">
            <div class="text-4xl mb-3">🕒</div>
            <h3 class="text-xl font-semibold mb-2">24/7 Available</h3>
            <p class="text-teal-100">Round-the-clock service whenever you need us</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Separator Callout -->
<section class="py-16">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div class="bg-white rounded-2xl shadow-lg p-8 border border-gray-100">
      <h2 class="text-3xl font-bold text-gray-900 mb-4">Ready to Ride?</h2>
      <p class="text-lg text-gray-600 mb-6">Fill out the booking form below and we'll have a taxi at your location in minutes!</p>
      <div class="flex justify-center">
        <svg class="w-8 h-8 text-blue-600 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </div>
    </div>
  </div>
</section>

<!-- Booking Form -->
<section id="booking" class="max-w-2xl mx-auto px-4">
  <div class="bg-white/95 backdrop-blur-sm rounded-xl p-8 shadow-2xl">
    <div class="text-center mb-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-2">Complete Your Booking</h2>
      <p class="text-gray-600">Fill out the form below and we'll confirm your booking via email</p>
    </div>

    <form action="/book" method="POST" novalidate class="space-y-6">
      <!-- Honeypot -->
      <input type="text" name="website" tabindex="-1" autocomplete="off" class="hidden" aria-hidden="true" />

      <!-- Personal Information -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <label for="fullName" class="block text-sm font-medium text-gray-700 mb-2">Full Name *</label>
          <input
            type="text"
            id="fullName"
            name="fullName"
            value="<%= formData.fullName || '' %>"
            placeholder="Enter your full name"
            aria-invalid="<%= errors.fullName ? 'true' : 'false' %>"
            class="form-field w-full px-4 py-3 rounded-lg border <%= errors.fullName ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
          <% if (errors.fullName) { %>
            <p class="mt-1 text-sm text-red-600"><%= errors.fullName.msg %></p>
          <% } %>
        </div>
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Email Address *</label>
          <input
            type="email"
            id="email"
            name="email"
            value="<%= formData.email || '' %>"
            placeholder="your.email@example.com"
            aria-invalid="<%= errors.email ? 'true' : 'false' %>"
            class="form-field w-full px-4 py-3 rounded-lg border <%= errors.email ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
          <% if (errors.email) { %>
            <p class="mt-1 text-sm text-red-600"><%= errors.email.msg %></p>
          <% } %>
        </div>
      </div>

      <div>
        <label for="phone" class="block text-sm font-medium text-gray-700 mb-2">Phone Number *</label>
        <input
          type="tel"
          id="phone"
          name="phone"
          value="<%= formData.phone || '' %>"
          placeholder="+1 (555) 123-4567"
          aria-invalid="<%= errors.phone ? 'true' : 'false' %>"
          class="form-field w-full px-4 py-3 rounded-lg border <%= errors.phone ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />
        <% if (errors.phone) { %>
          <p class="mt-1 text-sm text-red-600"><%= errors.phone.msg %></p>
        <% } %>
      </div>

      <!-- Trip Details -->
      <div class="border-t pt-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Trip Details</h3>
        <div class="space-y-4">
          <div>
            <label for="pickupAddress" class="block text-sm font-medium text-gray-700 mb-2">Pickup Address *</label>
            <input
              type="text"
              id="pickupAddress"
              name="pickupAddress"
              value="<%= formData.pickupAddress || '' %>"
              placeholder="123 Main St, City, State"
              aria-invalid="<%= errors.pickupAddress ? 'true' : 'false' %>"
              class="form-field w-full px-4 py-3 rounded-lg border <%= errors.pickupAddress ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
              required
            />
            <% if (errors.pickupAddress) { %>
              <p class="mt-1 text-sm text-red-600"><%= errors.pickupAddress.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="dropoffAddress" class="block text-sm font-medium text-gray-700 mb-2">Dropoff Address *</label>
            <input
              type="text"
              id="dropoffAddress"
              name="dropoffAddress"
              value="<%= formData.dropoffAddress || '' %>"
              placeholder="456 Oak Ave, City, State"
              aria-invalid="<%= errors.dropoffAddress ? 'true' : 'false' %>"
              class="form-field w-full px-4 py-3 rounded-lg border <%= errors.dropoffAddress ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
              required
            />
            <% if (errors.dropoffAddress) { %>
              <p class="mt-1 text-sm text-red-600"><%= errors.dropoffAddress.msg %></p>
            <% } %>
          </div>
        </div>
      </div>

      <!-- Date / Time / Passengers -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div>
          <label for="pickupDate" class="block text-sm font-medium text-gray-700 mb-2">Pickup Date *</label>
          <input
            type="date"
            id="pickupDate"
            name="pickupDate"
            value="<%= formData.pickupDate || '' %>"
            aria-invalid="<%= errors.pickupDate ? 'true' : 'false' %>"
            class="form-field w-full px-4 py-3 rounded-lg border <%= errors.pickupDate ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
          <% if (errors.pickupDate) { %>
            <p class="mt-1 text-sm text-red-600"><%= errors.pickupDate.msg %></p>
          <% } %>
        </div>
        <div>
          <label for="pickupTime" class="block text-sm font-medium text-gray-700 mb-2">Pickup Time *</label>
          <input
            type="time"
            id="pickupTime"
            name="pickupTime"
            value="<%= formData.pickupTime || '' %>"
            aria-invalid="<%= errors.pickupTime ? 'true' : 'false' %>"
            class="form-field w-full px-4 py-3 rounded-lg border <%= errors.pickupTime ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
          <% if (errors.pickupTime) { %>
            <p class="mt-1 text-sm text-red-600"><%= errors.pickupTime.msg %></p>
          <% } %>
        </div>
        <div>
          <label for="passengers" class="block text-sm font-medium text-gray-700 mb-2">Passengers *</label>
          <select
            id="passengers"
            name="passengers"
            aria-invalid="<%= errors.passengers ? 'true' : 'false' %>"
            class="form-field w-full px-4 py-3 rounded-lg border bg-white <%= errors.passengers ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          >
            <% for (let i = 1; i <= 6; i++) { %>
              <option value="<%= i %>" <%= (formData.passengers || 1) == i ? 'selected' : '' %>><%= i %></option>
            <% } %>
          </select>
          <% if (errors.passengers) { %>
            <p class="mt-1 text-sm text-red-600"><%= errors.passengers.msg %></p>
          <% } %>
        </div>
      </div>

      <!-- Notes -->
      <div>
        <label for="notes" class="block text-sm font-medium text-gray-700 mb-2">Special Notes (Optional)</label>
        <textarea
          id="notes"
          name="notes"
          rows="3"
          placeholder="Any special requests or additional information..."
          aria-invalid="<%= errors.notes ? 'true' : 'false' %>"
          class="form-field w-full px-4 py-3 rounded-lg border <%= errors.notes ? 'border-red-500 bg-red-50' : 'border-gray-300' %> focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none"
        ><%= formData.notes || '' %></textarea>
        <% if (errors.notes) { %>
          <p class="mt-1 text-sm text-red-600"><%= errors.notes.msg %></p>
        <% } %>
      </div>

      <div class="pt-2">
        <button type="submit" class="btn-primary w-full py-3 text-lg">Book My Taxi</button>
      </div>
    </form>
  </div>
</section>

